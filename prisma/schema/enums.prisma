enum ActiveStatus {
  active
  blocked
}

enum Role {
  user
  admin
  author
}

enum postStatus {
  PUBLISHED
  ARCHIVE
  DRAFT
}

enum commentStatus {
  APPROVED
  REJECTED
}

enum subscriptionStatus{
  ACTIVE
  CANCELLED
  EXPIRED

}
